---
import BuildLayout from '@/layouts/BuildLayout.astro';
import { getCollection } from 'astro:content';

const allPosts = await getCollection('posts', ({ data }) => data.world === 'build');
const posts = allPosts.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

const fmt = (d: Date) =>
  d.toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' });
---

<BuildLayout
  title="Writing on Web Development | Leigh Bertelsen"
  description="Notes on web development: authentication patterns, design sprints, and the language of working developers."
>
  <div class="placeholder">
    <h1 class="page-title">Writing</h1>

    {posts.length === 0 ? (
      <p class="empty">Writing is coming. Check back soon.</p>
    ) : (
      <ol class="post-list" role="list">
        {posts.map((post, i) => (
          <li class:list={['post-item', { 'has-divider': i > 0 }]}>
            <time class="post-date" datetime={post.data.date.toISOString()}>
              {fmt(post.data.date)}
            </time>
            <h2 class="post-title">
              <a href={`/blog/${post.slug}`}>{post.data.title}</a>
            </h2>
            <p class="post-excerpt">{post.data.excerpt}</p>
            <a class="post-link" href={`/blog/${post.slug}`}>Read →</a>
          </li>
        ))}
      </ol>
    )}
  </div>
</BuildLayout>

<style>
  .placeholder {
    max-width: 860px;
    margin-inline: auto;
    padding: 6rem 2rem 4rem;
  }
  @media (max-width: 767px) {
    .placeholder { padding: 5rem 1.5rem 6rem; }
  }

  .page-title {
    font-family: var(--font-sans);
    font-weight: 300;
    font-size: clamp(36px, 5vw, 56px);
    letter-spacing: -0.02em;
    color: var(--color-text-primary);
    margin: 0 0 3rem;
  }

  .empty {
    font-family: var(--font-sans);
    font-weight: 300;
    font-size: 15px;
    color: var(--color-text-secondary);
  }

  .post-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .post-item {
    padding: 2rem 0;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }
  .post-item.has-divider {
    border-top: 0.5px solid var(--color-border);
  }

  .post-date {
    font-family: var(--font-mono);
    font-weight: 400;
    font-size: 11px;
    color: var(--color-text-secondary);
    letter-spacing: 0.04em;
  }

  .post-title {
    margin: 0;
    font-family: var(--font-sans);
    font-weight: 500;
    font-size: 18px;
    color: var(--color-text-primary);
  }
  .post-title a {
    color: inherit;
    text-decoration: none;
    transition: color 200ms ease;
  }
  .post-title a:hover { color: var(--color-accent); }

  .post-excerpt {
    font-family: var(--font-sans);
    font-weight: 300;
    font-size: 14px;
    line-height: 1.6;
    color: var(--color-text-secondary);
    margin: 0.25rem 0 0;
    max-width: 65ch;
  }

  .post-link {
    font-family: var(--font-sans);
    font-weight: 400;
    font-size: 13px;
    color: var(--color-accent);
    text-decoration: none;
    margin-top: 0.25rem;
    align-self: flex-start;
    transition: opacity 200ms ease;
  }
  .post-link:hover { opacity: 0.75; }
</style>
